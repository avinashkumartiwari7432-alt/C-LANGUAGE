#include<stdio.h>   // It is header file.

int main(){     // it is the starting point of c program and execution starts from here.
    printf("Hello World");   //  It prints the text which is written inside the double quote.
    return 0;  // program ended successfully.
}
// Boilerplate code - That code which must be written .
