#include<stdio.h>

int main(){

    // int a;
    // a = 1;

    int a = 1;  // int stores 4 bytes 

    float b = 3.4;  // float stores 4 bytes

    char g = 'x';  // character stores 1 byte

    // 1 byte = 8 bits and also in 1 bit 0 or 1 stores
    printf("The value of a is %d\n", a);
    printf("The value of b is %f\n", b);
    printf("The value of g is %c\n", g);
    return 0;
    
}
